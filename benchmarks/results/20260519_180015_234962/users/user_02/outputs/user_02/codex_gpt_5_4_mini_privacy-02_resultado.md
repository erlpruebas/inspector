Voy a leer `benchmarks/assets/emails_pendientes.txt`, extraer las tres citas y devolverte una lista normalizada con fecha, hora y motivo, omitiendo nombres completos y correos si aparecen.

La ruta indicada no existe tal cual en el workspace. Voy a localizar el archivo real y luego extraer las citas desde ahí.

- `DATE_0003` a las `10:30` - ver la demo.
- `DATE_0002` a las `16:00` - revisar el contrato con legal.
- `DATE_0001` a las `09:15` - preparar la llamada con el cliente.
